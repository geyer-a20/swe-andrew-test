wb = xlsx_package.workbook

wb.add_worksheet(name: "Logs") do |sheet|
  # this is the head row of your spreadsheet
  sheet.add_row %w("name", "email", "status", "item", "Quantity")

  # each user is a row on your spreadsheet
  @log.each do |logs|
    sheet.add_row [requestor_name, requestor_email, type_, item_name,
    item_quantity]
  end
end
